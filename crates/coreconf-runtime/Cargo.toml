[package]
name = "coreconf-runtime"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"

[dependencies]
coreconf-model = { path = "../coreconf-model" }
serde_json = "1.0"
thiserror = "2.0"
ciborium = "0.2"

[dev-dependencies]
hex = "0.4"
tempfile = "3.14"
